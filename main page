<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="FTB Tennessee provides elite travel baseball opportunities for dedicated players across Tennessee.">
  <meta name="keywords" content="FTB Tennessee, travel baseball, elite baseball, Tennessee baseball, college commitments">
  <meta name="author" content="FTB Tennessee">
  <title>FTB Tennessee - Elite Travel Baseball</title>
  <!-- Open Graph and Twitter Card Meta Tags -->
  <meta property="og:title" content="FTB Tennessee - Elite Travel Baseball">
  <meta property="og:description" content="FTB Tennessee provides elite travel baseball opportunities for dedicated players across Tennessee.">
  <meta property="og:image" content="https://placehold.co/1200x600?text=FTB+Tennessee">
  <meta property="og:url" content="https://ftbtennessee.github.io">
  <meta property="og:type" content="website">
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="FTB Tennessee - Elite Travel Baseball">
  <meta name="twitter:description" content="FTB Tennessee provides elite travel baseball opportunities for dedicated players across Tennessee.">
  <meta name="twitter:image" content="https://placehold.co/1200x600?text=FTB+Tennessee">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <!-- Favicon -->
  <link rel="icon" type="image/x-icon" href="favicon.ico">
  <style>
    :root {
      --primary: #002D72; /* FTB Navy Blue */
      --secondary: #D50032; /* FTB Red */
      --accent: #32CD32; /* Lime Green */
      --light: #f8f9fa;
      --dark: #212529;
      --gray: #6c757d;
      --section-bg: #f5f5f5;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: 'Poppins', sans-serif;
      line-height: 1.8;
      color: var(--dark);
      margin: 0;
      padding: 0;
      background-color: white;
    }

    h1, h2, h3, h4, h5, h6 {
      font-family: 'Montserrat', sans-serif;
      font-weight: 700;
      text-transform: uppercase;
      margin: 0 0 15px;
    }

    a {
      text-decoration: none;
      transition: all 0.3s ease;
    }

    img {
      max-width: 100%;
      height: auto;
    }

    /* Animations */
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes scaleUp {
      from { transform: scale(1); }
      to { transform: scale(1.05); }
    }

    /* Header */
    .top-bar {
      background-color: var(--primary);
      color: white;
      padding: 10px 0;
      font-size: 14px;
      font-weight: 500;
      text-align: center;
    }

    .main-header {
      background-color: white;
      box-shadow: 0 4px 20px rgba(0,0,0,0.05);
      padding: 20px 0;
      position: sticky;
      top: 0;
      z-index: 1000;
    }

    .header-container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 20px;
    }

    .logo-container {
      display: flex;
      align-items: center;
    }

    .logo-container img {
      width: 50px;
      margin-right: 15px;
      border-radius: 5px;
    }

    .logo-text h1 {
      font-size: 36px;
      color: var(--primary);
      margin: 0;
    }

    .logo-text span {
      color: var(--primary);
    }

    /* Navigation */
    .nav-menu {
      display: flex;
      list-style: none;
      margin: 0;
      padding: 0;
    }

    .nav-menu li {
      margin-left: 35px;
    }

    .nav-menu li a {
      color: var(--dark);
      font-weight: 600;
      font-size: 16px;
      padding: 10px 15px;
      border-radius: 5px;
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    .nav-menu li a:hover,
    .nav-menu li a[aria-current="page"] {
      background-color: var(--primary);
      color: white;
    }

    /* Mobile Menu */
    .mobile-menu-btn {
      display: none;
      background: none;
      border: none;
      font-size: 30px;
      color: var(--primary);
      cursor: pointer;
      padding: 5px 10px;
    }

    @media (max-width: 768px) {
      .nav-menu {
        display: none;
        flex-direction: column;
        position: fixed;
        top: 0;
        right: 0;
        height: 100vh;
        width: 250px;
        background-color: white;
        box-shadow: -5px 0 15px rgba(0,0,0,0.1);
        padding: 80px 20px 20px;
        transform: translateX(100%);
        transition: transform 0.3s ease;
      }

      .nav-menu.active {
        display: flex;
        transform: translateX(0);
      }

      .nav-menu li {
        margin: 15px 0;
      }

      .nav-menu li a {
        font-size: 18px;
        padding: 15px 20px;
        display: block;
        border-radius: 8px;
        background-color: var(--light);
        color: var(--primary);
      }

      .nav-menu li a:hover,
      .nav-menu li a[aria-current="page"] {
        background-color: var(--primary);
        color: white;
      }

      .mobile-menu-btn {
        display: block;
      }

      .logo-text h1 {
        font-size: 28px;
      }

      .logo-container img {
        width: 40px;
      }
    }

    /* Hero Section */
    .hero {
      background: linear-gradient(rgba(0,45,114,0.9), rgba(0,45,114,0.9)), url('https://placehold.co/1920x800?text=Baseball+Field') no-repeat center center/cover;
      color: white;
      padding: 200px 0;
      text-align: center;
      position: relative;
    }

    .hero h1 {
      font-size: 64px;
      margin-bottom: 30px;
      animation: fadeInUp 1s ease forwards;
    }

    .hero p {
      font-size: 24px;
      font-weight: 400;
      max-width: 900px;
      margin: 0 auto 50px;
      animation: fadeInUp 1s ease forwards 0.3s;
    }

    @media (max-width: 768px) {
      .hero {
        padding: 120px 0;
      }

      .hero h1 {
        font-size: 40px;
      }

      .hero p {
        font-size: 18px;
        padding: 0 20px;
      }
    }

    /* Buttons */
    .btn {
      display: inline-block;
      padding: 16px 40px;
      border-radius: 50px;
      font-family: 'Montserrat', sans-serif;
      font-weight: 700;
      text-transform: uppercase;
      text-decoration: none;
      transition: all 0.3s ease;
      margin: 10px;
      animation: fadeInUp 1s ease forwards 0.6s;
    }

    .btn-primary {
      background-color: var(--secondary);
      color: white;
      border: 2px solid var(--secondary);
    }

    .btn-primary:hover {
      background-color: white;
      color: var(--secondary);
      transform: scale(1.05);
    }

    .btn-outline {
      background-color: transparent;
      color: white;
      border: 2px solid white;
    }

    .btn-outline:hover {
      background-color: white;
      color: var(--primary);
      transform: scale(1.05);
    }

    @media (max-width: 768px) {
      .btn {
        padding: 12px 30px;
        font-size: 14px;
      }
    }

    /* Main Content */
    .container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 100px 30px;
    }

    @media (max-width: 768px) {
      .container {
        padding: 60px 20px;
      }
    }

    .section-title {
      text-align: center;
      margin-bottom: 70px;
      animation: fadeInUp 1s ease forwards;
    }

    .section-title h2 {
      font-size: 48px;
      color: var(--primary);
      position: relative;
      display: inline-block;
      padding-bottom: 20px;
    }

    .section-title h2::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%);
      width: 120px;
      height: 5px;
      background-color: var(--secondary);
      border-radius: 2px;
    }

    @media (max-width: 768px) {
      .section-title h2 {
        font-size: 36px;
      }

      .section-title {
        margin-bottom: 50px;
      }

      .section-title h2::after {
        width: 80px;
        height: 4px;
      }
    }

    /* About Section */
    .about-section {
      background-color: white;
      padding: 100px 0;
    }

    .about-content {
      max-width: 900px;
      margin: 0 auto;
      text-align: center;
    }

    .about-content img {
      border-radius: 15px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.1);
      margin-bottom: 30px;
    }

    .about-text h3 {
      color: var(--primary);
      font-size: 36px;
    }

    .about-text p {
      margin: 20px 0;
      font-size: 18px;
      color: var(--gray);
      line-height: 1.9;
    }

    @media (max-width: 768px) {
      .about-section {
        padding: 60px 0;
      }

      .about-text h3 {
        font-size: 28px;
      }

      .about-text p {
        font-size: 16px;
      }
    }

    /* Teams Teaser */
    .teams-teaser {
      background-color: white;
      padding: 100px 0;
    }

    .teams-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 30px;
    }

    .team-card {
      background: white;
      border-radius: 15px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.05);
      padding: 30px;
      text-align: center;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .team-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    }

    .team-card h3 {
      color: var(--primary);
      font-size: 24px;
    }

    @media (max-width: 768px) {
      .teams-teaser {
        padding: 60px 0;
      }

      .team-card {
        padding: 20px;
      }

      .team-card h3 {
        font-size: 20px;
      }
    }

    /* Commitments Teaser */
    .commitments-teaser {
      background-color: var(--primary);
      color: white;
      padding: 100px 0;
    }

    .commitments-teaser .section-title h2 {
      color: white;
    }

    .commitments-teaser .section-title h2::after {
      background-color: var(--accent);
    }

    .commitments-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 30px;
    }

    .commitment-card {
      background: white;
      padding: 30px;
      border-radius: 15px;
      text-align: center;
      box-shadow: 0 5px 20px rgba(0,0,0,0.05);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      color: var(--dark);
    }

    .commitment-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    }

    .commitment-card h4 {
      color: var(--primary);
      font-size: 20px;
      margin: 0 0 10px;
    }

    .commitment-card p {
      font-weight: 600;
      color: var(--secondary);
      font-size: 18px;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .commitment-card img {
      width: 30px;
      margin-right: 10px;
    }

    .commitments-teaser .btn {
      margin-top: 40px;
      display: inline-block;
    }

    @media (max-width: 768px) {
      .commitments-teaser {
        padding: 60px 0;
      }

      .commitments-grid {
        gap: 20px;
      }

      .commitment-card {
        padding: 20px;
      }

      .commitment-card h4 {
        font-size: 18px;
      }

      .commitment-card p {
        font-size: 16px;
      }

      .commitment-card img {
        width: 25px;
      }
    }

    /* Footer */
    footer {
      background-color: var(--primary);
      color: white;
      padding: 60px 0;
    }

    .footer-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 40px;
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 30px;
    }

    .footer-section h4 {
      font-size: 22px;
      margin-bottom: 20px;
    }

    .footer-section p,
    .footer-section a {
      color: white;
      font-size: 16px;
      margin: 10px 0;
      opacity: 0.8;
    }

    .footer-section a:hover {
      color: var(--accent);
      opacity: 1;
    }

    .footer-links a {
      display: block;
      margin: 10px 0;
    }

    .footer-social .social-links {
      justify-content: flex-start;
    }

    .footer-social .social-links a {
      color: white;
      font-size: 24px;
    }

    .footer-social .social-links a:hover {
      color: var(--accent);
    }

    .copyright {
      text-align: center;
      margin-top: 40px;
      font-size: 14px;
      opacity: 0.7;
    }

    @media (max-width: 768px) {
      .footer-container {
        padding: 0 20px;
        gap: 30px;
      }

      .footer-section h4 {
        font-size: 20px;
      }

      .footer-section p,
      .footer-section a {
        font-size: 14px;
      }
    }

    /* Back to Top Button */
    .back-to-top {
      position: fixed;
      bottom: 40px;
      right: 40px;
      background-color: var(--secondary);
      color: white;
      width: 60px;
      height: 60px;
      border-radius: 50%;
      display: flex;
      justify-content: center;
      align-items: center;
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
      opacity: 0;
      transition: opacity 0.3s ease, transform 0.3s ease;
    }

    .back-to-top.visible {
      opacity: 1;
    }

    .back-to-top:hover {
      transform: scale(1.1);
      background-color: #e6003a;
    }

    @media (max-width: 768px) {
      .back-to-top {
        width: 50px;
        height: 50px;
        bottom: 20px;
        right: 20px;
      }

      .back-to-top i {
        font-size: 20px;
      }
    }
  </style>
</head>
<body>
  <!-- Top Bar -->
  <div class="top-bar">
    Elite Travel Baseball
  </div>

  <!-- Main Header -->
  <header class="main-header" role="banner">
    <div class="header-container">
      <div class="logo-container">
        <img src="images/ftb-logo.png" alt="FTB Tennessee Logo" loading="lazy">
        <div class="logo-text">
          <h1>FTB <span>TENNESSEE</span></h1>
        </div>
      </div>
      <button class="mobile-menu-btn" onclick="toggleMobileMenu()" aria-expanded="false" aria-controls="navMenu" aria-label="Toggle navigation menu">
        <i class="fas fa-bars"></i>
      </button>
      <nav>
        <ul class="nav-menu" id="navMenu">
          <li><a href="index.html" aria-current="page">Home</a></li>
          <li><a href="about.html">About</a></li>
          <li><a href="teams.html">Teams</a></li>
          <li><a href="commitments.html">Commitments</a></li>
          <li><a href="contact.html">Contact</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <!-- Hero Section -->
  <section class="hero" id="home">
    <div class="container">
      <h1>FTB Tennessee</h1>
      <p>Empowering Elite Baseball Players with Top-Tier Training and Competition</p>
      <div>
        <a href="teams.html" class="btn btn-primary">View Teams</a>
        <a href="contact.html" class="btn btn-outline">Join Our Team</a>
      </div>
    </div>
  </section>

  <!-- About Section (Shortened) -->
  <section class="about-section" id="about">
    <div class="container">
      <div class="section-title">
        <h2>About Us</h2>
      </div>
      <div class="about-content">
        <div class="about-text">
          <h3>Our Mission</h3>
          <img src="https://placehold.co/600x400?text=FTB+Team+in+Action" alt="FTB Tennessee team in action" loading="lazy">
          <p>FTB Tennessee is dedicated to developing elite baseball players through discipline, hard work, and competitive excellence.</p>
          <a href="about.html" class="btn btn-primary">Learn More</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Teams Teaser -->
  <section class="teams-teaser" id="teams">
    <div class="container">
      <div class="section-title">
        <h2>Our Teams</h2>
      </div>
      <div class="teams-grid">
        <div class="team-card">
          <h3>12U Teams</h3>
        </div>
        <div class="team-card">
          <h3>13U Teams</h3>
        </div>
        <div class="team-card">
          <h3>14U Teams</h3>
        </div>
        <div class="team-card">
          <h3>2028 Teams (15U)</h3>
        </div>
        <div class="team-card">
          <h3>2027 Teams (16U)</h3>
        </div>
        <div class="team-card">
          <h3>2026 Teams</h3>
        </div>
      </div>
      <div style="text-align: center;">
        <a href="teams.html" class="btn btn-primary" style="margin-top: 40px;">View Schedules</a>
      </div>
    </div>
  </section>

  <!-- Commitments Teaser -->
  <section class="commitments-teaser" id="commitments">
    <div class="container">
      <div class="section-title">
        <h2>Recent College Commitments</h2>
      </div>
      <div class="commitments-grid">
        <div class="commitment-card">
          <h4>Cayden King (2026)</h4>
          <p><img src="https://placehold.co/30x30?text=TAM" alt="Texas A&M Logo" loading="lazy">Texas A&M</p>
        </div>
        <div class="commitment-card">
          <h4>Kael Barney (2026)</h4>
          <p><img src="https://placehold.co/30x30?text=OSU" alt="Oregon State Logo" loading="lazy">Oregon State</p>
        </div>
        <div class="commitment-card">
          <h4>Max Riggs (2026)</h4>
          <p><img src="https://placehold.co/30x30?text=QU" alt="Queen University Logo" loading="lazy">Queen University</p>
        </div>
      </div>
      <div style="text-align: center;">
        <a href="commitments.html" class="btn btn-outline">View All Commitments</a>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer role="contentinfo">
    <div class="footer-container">
      <div class="footer-section">
        <h4>FTB Tennessee</h4>
        <p>Elite Travel Baseball</p>
        <p>Email: <a href="mailto:ftbtennessee@gmail.com">ftbtennessee@gmail.com</a></p>
      </div>
      <div class="footer-section">
        <h4>Quick Links</h4>
        <div class="footer-links">
          <a href="index.html">Home</a>
          <a href="about.html">About</a>
          <a href="teams.html">Teams</a>
          <a href="commitments.html">Commitments</a>
          <a href="contact.html">Contact</a>
        </div>
      </div>
      <div class="footer-section footer-social">
        <h4>Follow Us</h4>
        <div class="social-links">
          <a href="https://www.instagram.com/ftb_tennessee/" aria-label="Instagram"><i class="fab fa-instagram"></i></a>
          <a href="https://x.com/ftb_tennessee" aria-label="Twitter"><i class="fab fa-twitter"></i></a>
          <a href="https://www.facebook.com/FTBTennessee/" aria-label="Facebook"><i class="fab fa-facebook-f"></i></a>
        </div>
      </div>
    </div>
    <div class="copyright">
      © 2025 FTB Tennessee. All rights reserved.
    </div>
  </footer>

  <!-- Back to Top Button -->
  <a href="#home" class="back-to-top" aria-label="Back to top">
    <i class="fas fa-chevron-up"></i>
  </a>

  <script>
    // Toggle mobile menu
    function toggleMobileMenu() {
      const navMenu = document.getElementById('navMenu');
      const menuBtn = document.querySelector('.mobile-menu-btn');
      const isExpanded = menuBtn.getAttribute('aria-expanded') === 'true';
      navMenu.classList.toggle('active');
      menuBtn.setAttribute('aria-expanded', !isExpanded);
    }

    // Back to top button visibility
    window.addEventListener('scroll', () => {
      const backToTop = document.querySelector('.back-to-top');
      if (window.scrollY > 300) {
        backToTop.classList.add('visible');
      } else {
        backToTop.classList.remove('visible');
      }
    });

    // Highlight active nav link on scroll
    window.addEventListener('scroll', () => {
      const sections = document.querySelectorAll('section[id]');
      const navLinks = document.querySelectorAll('.nav-menu a');
      let currentSection = '';

      sections.forEach(section => {
        const sectionTop = section.offsetTop - 100;
        if (window.scrollY >= sectionTop) {
          currentSection = section.getAttribute('id');
        }
      });

      navLinks.forEach(link => {
        link.removeAttribute('aria-current');
        if (link.getAttribute('href').includes(currentSection)) {
          link.setAttribute('aria-current', 'page');
        }
      });
    });
  </script>
</body>
</html>
